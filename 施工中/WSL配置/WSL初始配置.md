# 初始配置

**字体**：
刚打开可能会报缺少ubuntu mono字体，运行一下代码就不报了，可能是解决方法？不确定。

```bash
sudo apt update -y
sudo apt upgrade -y
```

**更新软件包**：

```bash
sudo apt update -y
sudo apt upgrade -y
# 安装编译工具链（gcc/make等）
sudo apt install -y build-essential
```

